import styled from 'styled-components';

import { Container } from '@app/components/container';
import { spacing12 } from '@app/styles/spacing.ts';

export const StyledUsersPage = styled(Container)`
  margin-top: ${spacing12};
  height: 100%;
`;
