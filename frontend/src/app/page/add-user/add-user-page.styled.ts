import styled from 'styled-components';

import { Container } from '@app/components/container';
import { spacing4 } from '@app/styles/spacing.ts';

export const StyledCreateUserPage = styled(Container)`
  margin-top: ${spacing4};
  margin-bottom: ${spacing4};
`;
